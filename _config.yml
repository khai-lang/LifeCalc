title: LifeCalc
description: 생활 계산기 포털 — 급여·세금, 부동산·대출, 투자, 건강, 날짜/기념일
url: https://calculator.khaistory.com
lang: ko-KR
timezone: Asia/Seoul

theme: minima   # ← 여기 반드시 추가!!

plugins:
  - jekyll-redirect-from
  - jekyll-sitemap
  - jekyll-seo-tag
  - jekyll-paginate-v2

# 들여쓰기를 지우고 가장 왼쪽(root)에 작성하세요
pagination:
  enabled: true
  collection: 'posts' # 전체 글 중
  categories: ['guide'] # 'guide' 카테고리만 필터링!
  per_page: 9
  permalink: '/lifeplus/guide/page:num/'
   
markdown: kramdown
permalink: /:path/

kramdown:
  input: GFM
  hard_wrap: false
  toc_levels: 2..3

defaults:
  - scope:
      path: ""
      type: pages
    values:
      layout: default
      lang: ko-KR
      sitemap: true

  - scope: { path: "life" }
    values: { section: "life" }
  - scope: { path: "salary" }
    values: { section: "salary" }
  - scope: { path: "realestate" }
    values: { section: "realestate" }
  - scope: { path: "invest" }
    values: { section: "invest" }
  - scope: { path: "insurance" }
    values: { section: "insurance" }
  - scope: { path: "guide" }
    values: { section: "guide" }

exclude:
  - node_modules
  - vendor
  - Gemfile
  - Gemfile.lock
  - package.json
  - package-lock.json
  - yarn.lock
  - README.md
  - scripts
  - tmp
  - tests
  - .github
  - .sass-cache
